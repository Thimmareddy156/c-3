# c-3
Arg -no-return.c
#include <stdio.h>
void tables(int){
    for(int i=1;i<=10;i++){
        printf("%dx %d = %d\n",i,n*i);
    }
}
int main(){
    int n;
    printf("Enter the value of n:");
    scanf("%d",&n);
    tables(n);
    return 0;
}
